[package]
name = "termal-alignment"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/sib-swiss/termal"
description = "Alignment parsing and metrics for the termal multiple-sequence-alignment viewer"

[dependencies]
itertools = "0.14.0"

[dev-dependencies]
approx = "0.3.2"    # needed for testing vec_f64_aux
